services:
  cvless:
    image: bretfisher/jekyll-serve
    ports:
      - "4000:4000"
    volumes:
      - .:/site
    command: bundle exec jekyll serve --host 0.0.0.0 --config _config.yml,_config.dev.yml
